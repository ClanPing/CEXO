# Bulleen Scripts

Post-processing and manuscript figure helpers live here. The normal entrypoint is still:

```powershell
python run_bulleen_cexo_pipeline.py
```

That wrapper calls these scripts with paths rooted at `examples/bulleen_study`.
