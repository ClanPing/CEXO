name: cexo
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.20.0
  - matplotlib>=3.5.0
  - pip

  - pip:
      - torch>=2.0.0
      - tqdm>=4.62.0
      - scikit-learn>=1.0.0
